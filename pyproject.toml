[tool.poetry]
name = "project"
version = "0.1.0"
description = ""
authors = ["Euclides Formica <eformica@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
pika = "^1.3.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
